import { Accessibility } from '../../types';
/**
 * @description
 * Behavior for a card component - semantic grouping of objects
 * @specification
 * Adds role='group'.
 */
declare const cardBehavior: Accessibility<CardBehaviorProps>;
export default cardBehavior;
export declare type CardBehaviorProps = never;
